function stringLength(strOne, strTwo, strThree) {
    let result = strOne.length + strTwo.length + strThree.length;
    console.log(result);
    console.log(Math.floor(result / 3));

}
stringLength('chocolate', 'ice cream', 'cake');
stringLength('pasta', '5', '22.3'); 